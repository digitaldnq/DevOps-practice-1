# DevOps-practice-1
# Jenkins Pipeline для автоматизации сборки 

![Jenkins](https://img.shields.io/badge/Jenkins-D33833?style=for-the-badge&logo=Jenkins&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white)
## 📋 Кракое описание

Этот проект представляет собой **Jenkins Pipeline**, предназначенный для полной автоматизации процесса сборки, тестирования и доставки приложения. Pipeline реализован с использованием **Jenkinsfile** и технологии **Declarative Pipeline**.

**Основная цель** - обеспечить непрерывную интеграцию и непрерывную доставку (CI/CD), минимизируя ручное вмешательство и снижая количество ошибок.
### 🔧 Используемые шаги и инструменты:

*   **SCM**: Git для контроля версий.
*   **Сборка**: Maven / Gradle (в зависимости от проекта).
*   **Тестирование**: Запуск юнит-тестов и интеграционных тестов.
*   **Анализ кода**: SonarQube для проверки качества кода.
*   **Упаковка**: Создание Docker-образа.
*   **Публикация**: Загрузка артефактов в Nexus/Artifactory и Docker-образа в Docker Hub/Registry.
*   **Уведомления**: Отправка статуса сборки в Slack.

---

## 🔧 Стадии Pipeline

В таблице ниже подробно описаны все стадии pipeline и задачи, которые они выполняют.

| Стадия | Задачи | Ключевые инструменты |
| :--- | :--- | :--- |
| **`Checkout`** | Получение исходного кода из репозитория. | `git`, `checkout` |
| **`Build`** | Компиляция исходного кода и сборка артефакта. | `maven` / `gradle` |
| **`Unit Test`** | Запуск набора юнит-тестов и генерация отчетов. | `junit`, `jacoco` |
| **`Code Analysis`** | Статический анализ кода на качество и уязвимости. | `sonarqube` |
| **`Package`** | Создание Docker-образа на основе собранного артефакта. | `docker` |
| **`Publish`** | Публикация артефакта в репозиторий и Docker-образа в registry. | `nexus`, `docker` |
| **`Deploy to Dev`** | ***(Опционально)*** Автоматическое развертывание в среду разработки. | `ssh`, `kubectl` |
| **`Notify`** | Отправка уведомления о результатах выполнения pipeline. | `slackSend`, `email` |

---

## 🔧 Пример Jenkinsfile

Ниже представлен пример кода `Jenkinsfile`:

```groovy
pipeline {
    agent any
    stages {
        stage('Build & Test') {
            steps {
                checkout scm
                sh 'mvn clean package'
            }
        }
    }
}

##  Инструкции по запуску и настройке

###  🔧 Предварительные требования

**Плагины Jenkins:**
- Pipeline
- Git  
- Maven Integration *(опционально)*

** 🔧 Настройки:**
- JDK в `Управление Jenkins → Настройки системы`
- Учетные данные для Git репозитория

###  Быстрой старт

1. **Создайте** новый Pipeline Item
2. **Выберите** `Pipeline script from SCM`
3. **Укажите** ваш Git репозиторий
4. **Сохраните** и запустите сборку 
